grumphp:
    hooks_dir: ~
    fixer:
        enabled: true
        fix_by_default: false
    tasks:
        phpcs: ~
        phpcsfixer: ~
        phpunit: ~
        git_commit_message:
            matchers:
                'Penser à mettre le numéro de issue (ex: "DEV-XXX: Lorem ipsum")': /((DEV|RUN)-\d+:.+)/
